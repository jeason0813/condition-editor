import '../data/datastore';

export const PRODUCTS = window.datastore.getProducts();
export const PROPERTIES = window.datastore.getProperties();
export const OPERATORS = window.datastore.getOperators();
